{
  "name": "hypefeed-worker",
  "version": "0.1.0",
  "description": "AI YouTube channels RSS feed aggregation service on Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "bun run build:worker",
    "build:worker": "esbuild src/index.ts --bundle --outfile=dist/worker.js --format=esm --target=es2022",
    "type-check": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.6.8",
    "fast-xml-parser": "^4.5.0",
    "date-fns": "^4.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "wrangler": "^3.95.0",
    "typescript": "^5.7.2",
    "esbuild": "^0.24.2",
    "@types/node": "^22.10.2"
  },
  "keywords": [
    "cloudflare-workers",
    "rss",
    "youtube",
    "ai",
    "aggregation",
    "hono"
  ],
  "author": "HypeFeed",
  "license": "MIT"
}